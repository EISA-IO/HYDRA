{
  "description": "Native toolchain bundled with Hydra so tools work with no manual download.",
  "tools": {
    "rtk": {
      "role": "input compression (Rust Token Killer)",
      "bundled": { "mac-arm64": "mac-arm64/rtk", "mac-x64": "mac-x64/rtk", "win-x64": "win-x64/rtk.exe" },
      "version": "0.43.0",
      "fallbackInstall": "curl -fsSL https://raw.githubusercontent.com/reachingforthejack/rtk/main/install.sh | sh"
    },
    "caveman": {
      "role": "output compression (Claude Code plugin)",
      "bundledMarketplace": "caveman",
      "localInstall": "node caveman/bin/install.js --only claude --only codex",
      "fallbackInstall": "npx -y github:JuliusBrussee/caveman --only claude --only codex"
    },
    "claude-video": {
      "role": "video understanding skill (/watch)",
      "bundledMarketplace": "claude-video",
      "bundledSkill": "claude-video/skills/watch",
      "localInstall": "copy claude-video/skills/watch to ~/.claude/skills/watch and ~/.agents/skills/watch",
      "fallbackInstall": "npx skills add bradautomates/claude-video -g"
    },
    "claude": {
      "role": "Claude Code CLI (Anthropic — not redistributable, auto-installed once if missing)",
      "bundled": {},
      "fallbackInstall": "curl -fsSL https://claude.ai/install.sh | bash"
    },
    "headroom": {
      "role": "optional compression proxy (Python)",
      "bundled": {},
      "fallbackInstall": "pipx install 'headroom-ai[all]'"
    }
  }
}
